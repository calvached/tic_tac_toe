#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'round'
require 'input_output'
require 'configurations'

round = Round.new(Configurations.new(MyIO.new), MyIO.new)
round.start_game
